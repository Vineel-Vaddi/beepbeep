# Phase-1 placeholder (readiness only)

BOX_CLASSES = ["plate", "bike", "rider_head", "rider_hand", "traffic_signal"]

def export_yolo(*args, **kwargs):
    """
    Phase-2: generate YOLO folders:
      images/train, images/val, labels/train, labels/val, data.yaml
    using the stored normalized (x,y,w,h) center format.
    """
    raise NotImplementedError("YOLO export will be implemented in Phase-2.")
